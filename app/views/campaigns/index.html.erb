<body class="bg-black text-white">
  <div class="container mt-5 pt-5 d-flex flex-column align-items-center">
    <h1 class="text-center">CAMPAIGNS</h1>
    <h3 class="text-center p-3">Radiant uses details from your game session to craft a personalized introduction to your campaign. To get started, create a new campaign, or click "Existing Campaigns" to select from previous ones.</h3>
    <hr class="border border-top border-success border-2 w-100 mb-5">
    <%= link_to new_campaign_path(@campaign), class: 'text-decoration-none link-dark' do %>
      <div class="btn btn-success p-3 m-5" style="border-radius: 24px">
        Create New Campaign
      </div>
    <% end %>
    <div class="accordion">
      <div class="accordion-item">
        <h2 class="accordion-header" id="headingOne">
          <button class="accordion-button collapsed" style="border-radius: 24px"type="button" data-bs-toggle="collapse" data-bs-target="#collapseOne" aria-expanded="false" aria-controls="collapseOne">
            Existing Campaigns
          </button>
        </h2>
        <div id="collapseOne" class="accordion-collapse collapse" aria-labelledby="headingOne" data-bs-parent="#accordionExample">
          <div class="accordion-body">
            <div class="container d-flex align-items-center justify-content-center flex-wrap p-3">
              <% @campaigns.each do |campaign| %>
                <div class="campaign-card relative">
                  <div class="card-world-name">
                    <%=campaign.world_name.capitalize %>
                  </div>
                  <%= link_to campaign_path(campaign), class: "text-decoration-none" do %>
                    <div>
                      <%=image_tag("#{campaign.world_biome}.jpg")%>
                    </div>
                  <% end %>
                  <%=link_to campaign_path(campaign), class: "absolute", data: {turbo_method: :delete, turbo_confirm: "Do you want to delete this campaign?"} do %>
                    <i class="fa-solid fa-trash-can text-muted m-2"></i>
                  <% end %>
                </div>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
  <%#= link_to 'Back to the homepage', root_path, class: 'btn world-button w-50'   %>
